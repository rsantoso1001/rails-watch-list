<h1><li><%= @list.name %></li></h1>

<%= link_to "Create a bookmark", new_list_bookmark_path(@list) %>

<ul>
  <% @list.bookmarks.each do |bookmark| %>
<li> <%= bookmark.movie.title %> - <%= bookmark.comment %> </li>
  <% end %>
</ul>
